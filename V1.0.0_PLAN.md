# V1.0.0 计划：仓库理解优先的项目考核面试生成器

## 1. 当前判断

V1.0.0 的第一优先级不是 JD 匹配，也不是同时覆盖所有输入形态，而是先把 **GitHub 仓库理解** 做扎实。alpha.1 的重点进一步收窄为：把论文 / AI 项目制代码库读对，而不是做通用软件工程审查。

原因：

- 真正的项目拷打首先来自材料本身，而不是岗位描述。
- 如果仓库理解不准，JD 只会把泛问题包装成岗位化问题。
- DevContext.AI 证明了“仓库分析 -> 报告 -> 面试问题”这条路线可行，但我们的目标要从软件工程仓库分析改造成算法岗项目考核。
- 研究岗 / 算法岗 / 保研场景里，面试官会先确认候选人是否真的懂自己的方法、实验、配置、数据和复现，再切到原理、八股和场景迁移。

因此 V1.0.0 先做：

- 必要输入：GitHub 仓库链接。
- 核心能力：仓库抓取、paper-code 文件筛选、结构化理解、可追问点审查、证据绑定问题生成。
- 题库层：`kaomian` 作为补充校准器，但不能盖过仓库理解。

暂时后置：

- JD / 岗位描述。
- arXiv 论文解析。
- 实时聊天式面试。
- `bagu-killer` 定时更新题库。

## 2. 一句话定位

> 输入 GitHub 仓库，系统先按论文 / AI 项目制代码库读懂方法 claim、核心代码、训练配置、数据处理、评测逻辑和复现路线，再生成一份有证据来源的算法岗项目考核面试计划。

## 3. V1.0.0 核心架构

```text
必要输入：GitHub 仓库链接

        ↓

仓库抓取与文件筛选
        ↓
仓库结构化理解
        ↓
项目考核点审查
        ↓
检索 kaomian 高频八股
        ↓
连接仓库证据与八股
        ↓
生成项目考核面试计划
```

后续增强：

```text
v1.0.0-alpha：只支持 GitHub repo 理解
v1.0.0-alpha.1：GitHub repo paper-code 理解
v1.0.0-beta：接入 kaomian
v1.0.0-rc：支持 GitHub repo + 一句话自述
v1.1.0：增加 arXiv / 论文项目理解
v1.2.0：增加 JD / 岗位描述偏置
v1.3.0：增加实时追问
v1.4.0：接入 bagu-killer 定时更新题库
```

## 4. 模块拆分

### 4.1 输入解析

V1.0.0 只要求处理 GitHub 仓库链接。

职责：

- 校验 GitHub URL。
- 解析 owner、repo、branch。
- 判断公开仓库是否可访问。
- 记录用户可选补充，例如“我负责的模块”或“目标方向”。

输出：

- `source_type = github`
- `repository_url`
- `owner`
- `repo`
- `branch`
- `user_focus`
- `domain_hint`

### 4.2 仓库抓取

职责：

- 获取 README。
- 获取文件树。
- 获取关键源码文件。
- 获取配置文件、测试文件、评测脚本、示例数据说明。
- 排除依赖目录、锁文件、构建产物、图片、二进制文件、大文件和自动生成文件。
- alpha.1 继续使用 GitHub REST API，不使用 `git clone`，降低本地运行环境复杂度。
- `GITHUB_TOKEN` 用于提升 rate limit：未认证通常 60 次/小时/IP，认证请求常规 5000 次/小时。

优先级：

1. README / paper docs / citation / project page。
2. 训练、推理、评测和 reproduce 入口。
3. configs / hyperparameters / requirements。
4. models / losses / algorithms / methods / agent / RAG / RL / diffusion / RLHF。
5. datasets / dataloader / preprocess / tokenizer。
6. eval / metrics / benchmark / ablation。
7. scripts / notebooks / demo。

alpha.1 必须对这些类别做 bucket 控制，避免 benchmark label、测试数据或 docs 把方法实现挤出上下文。

### 4.3 paper-code 仓库结构化理解

这一层是 V1.0.0 的核心，不允许跳过。

Step 1 改为“论文项目理解”。它不是通用代码摘要，也不是系统用正则先判断 paper signals，而是把 README、链接、文件树和代码证据交给模型，让模型自己做 repo deep research。模型先根据论文 / AI 项目的类型选择轻量理解 skill，再抽取问题设定、方法主张、核心贡献、训练 / 推理流程、数据流、实验评测、复现入口和关键超参。

alpha.1 先内置轻量 skill registry，不做复杂插件系统。后续每个 skill 都可以替换成更高级的 agent、工具链或专门模型：

- `benchmark-skill`：理解 benchmark / eval repo，重点看数据集定义、metric、baseline、leaderboard、reproduce、data leakage 和评测协议。
- `training-skill`：理解 train / finetune repo，重点看训练入口、loss、optimizer、scheduler、config、seed、checkpoint、数据加载和超参。
- `inference-skill`：理解 inference / demo repo，重点看输入输出、权重加载、采样策略、后处理、速度、显存和失败样例。
- `method-skill`：理解 method / model repo，重点看核心算法、模型结构、loss、policy、reward、retrieval、agent loop 和替代 baseline。
- `data-skill`：理解 dataset / preprocessing repo，重点看数据来源、清洗、split、tokenizer、label、annotation 和泄漏风险。
- `reproduce-skill`：理解 reproduce / script-heavy repo，重点看命令链、环境、依赖、配置组合、结果文件和复现实验缺口。

skill 选择规则：

- 一个仓库可以命中多个 skill。
- 命中结果只影响“如何读仓库”和“如何出题”，不改变 alpha.1 的输入形态。
- 每个 skill 输出都必须绑定文件证据，不能只凭仓库名或 README 推断。
- 如果无法明确分类，走 `paper-code-general-skill`，仍然按方法、训练、评测、数据、配置和复现六类证据审查。
- `analysisMode` 和 `paperSignals` 由模型基于原始证据判断，不由 GitHub ingest 阶段做语义解析。
- 模型请求不设置 `max_tokens`，README 不做 30k 字符截断；但模型服务自身仍然有上下文窗口和超时上限。

输出必须包含：

- 分析模式：`paper-code` / `general-code` / `unknown`。
- 问题设定：这个仓库对应什么任务或研究问题。
- Paper claim：README / paper docs 声称的核心贡献和效果。
- 用户贡献假设：哪些方法、实验或数据处理部分最可能被追问。
- 技术栈：语言、框架、模型、训练工具、评测工具。
- 入口路径：训练、推理、评测、reproduce 从哪里开始。
- 核心方法模块：每个模块对应什么方法概念。
- 数据流：dataset、preprocess、dataloader、model、loss、metric 怎么连接。
- 模型或 agent loop：如果存在，拆出 loop、state、tool、memory、eval。
- 评测逻辑：baseline、ablation、benchmark、metrics、demo case。
- 复现风险：配置、seed、数据依赖、权重、脚本缺失、指标不稳定。

### 4.4 项目考核点审查

审查智能体要像认真算法岗面试官、导师或 paper reviewer 一样找可追问点。

GitHub 可出题点：

- 这个项目真正的方法难点在哪里。
- Paper claim 和代码实现是否一致。
- 训练、推理、评测路线是否能复现。
- baseline、ablation、metric 是否支撑结论。
- 是否存在数据泄漏、配置隐藏、seed 不稳定、评测脚本不完整。
- 如果项目是 AI / Agent / RAG / RL / diffusion / RLHF，模型、数据、loss、policy、reward、eval、推理链路是否讲得清。
- 哪些地方最容易被问“为什么这样设计，而不是用更简单 baseline”。
- 哪些地方会暴露“可能不是自己做的”。

### 4.5 kaomian 题库检索

V1.0.0 使用 `kaomian` 的 Markdown 题库快照，但它只能作为补充校准器。

推荐优先使用：

- `01_Top100_高频题.md`
- `03_Agent_RAG_Tool_Memory.md`
- `05_机器学习_大模型手撕.md`
- `06_项目拷打题.md`

检索输入：

- 仓库技术标签。
- 仓库审查出的风险点。
- 核心模块涉及的原理关键词。

检索输出：

- 相关高频题。
- 相关岗位 / 公司出现情况，如果题库里有。
- 题型。
- 可连接的项目证据。

### 4.6 仓库证据与八股连接

这是出题生成前的关键步骤。

系统不能直接把 `kaomian` 题拼到面试里，而要把题库问题改写成项目相关问题。

例子：

```text
仓库证据：代码里有 RAG 召回模块，但没有 eval 或 bad case 分析。
kaomian 召回：RAG 召回 bad case 怎么处理？
生成问题：你这个仓库里的 RAG 召回失败时，怎么判断是 chunk 策略、embedding 模型还是 rerank 逻辑的问题？
```

如果题库题找不到仓库证据，只能放到“补充八股”，不能进入主追问链。

## 5. 最终输出格式

V1.0.0 先输出“项目考核面试计划”，而不是完整聊天系统。

建议结构：

```text
1. 仓库 / 论文身份识别
2. Paper claim 摘要
3. 训练、推理、评测复现路线
4. 核心方法代码地图
5. 实验与评测证据地图
6. Paper claim -> Code evidence -> Interview risk
7. 可出题点列表
8. 分层面试问题
9. 每题证据来源
10. 每题期望回答
11. 红旗回答
12. 追问链
13. 面试前补坑建议
```

每道题建议包含：

- 问题。
- 问题类型。
- 难度。
- 仓库证据来源。
- 为什么会问。
- 关联的高频题。
- 期望回答要点。
- 红旗回答。
- 后续追问。

## 6. V1.0.0 最小可行版本

为了尽快落地，V1.0.0 可以先做成“生成报告”：

1. 用户输入 GitHub 仓库链接。
2. 可选填写“我负责的模块 / 目标方向”，不接 JD。
3. 点击生成。
4. 系统输出一份 Markdown 项目考核面试计划。

暂时不做：

- JD / 岗位匹配。
- arXiv 论文解析。
- 实时聊天。
- 用户登录。
- 历史记录。
- 视频 / 语音。
- 自动运行 `bagu-killer`。
- 在线更新题库。

## 7. 后续版本

### v1.0.0-alpha：GitHub repo 理解

- 输入 GitHub 仓库链接。
- 输出仓库理解报告和基础项目考核面试计划。
- 不接 `kaomian`，不接用户自述，不接 arXiv，不接 JD。

### v1.0.0-alpha.1：GitHub repo paper-code 理解

- 不改变输入形态，仍然只输入公开 GitHub 仓库链接。
- 自动识别 paper-code / general-code / unknown。
- 文件筛选优先覆盖 README / paper docs、configs、train、infer、eval、data、method、scripts。
- 输出 paper claim、方法代码地图、训练 / 评测复现路线和算法岗项目追问。
- 不接 `kaomian`，不接用户自述，不接 arXiv，不接 JD。

### v1.0.0-beta：接入 kaomian

- 用仓库技术标签和风险点检索 `kaomian`。
- 把高频题改写成绑定仓库证据的项目追问。
- 区分主追问链和补充八股。

### v1.0.0-rc：GitHub repo + 一句话自述

- 用户可以补一句“我主要做了哪些部分”。
- 系统用这句话聚焦审查对象。
- 一句话自述不能替代仓库证据。

### v1.1.0：arXiv / 论文项目理解

- 增加 arXiv 链接输入。
- 解析论文动机、方法、实验、指标、局限和相关工作。
- 把论文证据接入项目考核面试计划。

### v1.2.0：JD 偏置

- 增加 JD / 岗位描述输入。
- 把仓库理解结果映射到岗位能力要求。
- 输出“这个岗位最可能追问仓库里的哪些部分”。

### v1.3.0：实时面试追问

- 用户可以选择某个问题开始连续追问。
- 每轮回答后给出评分和下一轮追问。
- 引入 answer evaluation 和 follow-up loop。

### v1.4.0：题库定时更新

- 定时运行 `bagu-killer`。
- 自动更新题库。
- 记录不同时间段高频题变化。

## 8. 风险和解决方案

### 风险 1：仓库太大

解决：

- 先读 README 和文件树。
- 只抓核心文件。
- 按文件类型、路径、import 关系和 README 提及频次打分。
- 大文件跳过。

### 风险 2：仓库理解太浅

解决：

- 输出必须包含项目主流程、核心模块、数据流和评测逻辑。
- 不允许只根据 README 生成问题。
- 问题必须引用仓库证据。

### 风险 3：题库和项目两张皮

解决：

- 每道主问题必须绑定仓库证据。
- 如果找不到证据，只能作为“补充八股”，不能进入主面试计划。

### 风险 4：问题太泛

解决：

- 每个问题必须包含“为什么问”和“证据来源”。
- 禁止输出没有材料依据的泛泛问题。

### 风险 5：AI / 研究项目被当成普通 Web 项目

解决：

- 仓库理解阶段必须识别模型、数据、agent loop、评测、推理和训练相关文件。
- 对 AI / Agent / RAG / RL 项目使用专门审查维度。
- 对保研 / 研究岗场景保留“为什么这么设计、实验是否可信、细节是否真懂”的追问方向。

## 9. 当前最重要的产品原则

- 仓库理解优先。
- 证据优先。
- 题库只是校准器，不是主线。
- JD 是后续偏置层，不是 V1.0.0 必要能力。
- 先生成高质量面试计划，再做实时聊天。
- 不要为了演示效果牺牲问题的可信度。
