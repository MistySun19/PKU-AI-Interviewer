# V1.0.0 Plan：Traceback Repo 面试风险审查器

## Summary

V1.0.0 从完整 AI 面试平台收缩为 **Repo 面试风险审查器**。

用户输入 GitHub repo，系统输出：

> 这是你的项目里最可能被面试官问穿的 k 个地方。

每个风险点必须绑定代码、README 或配置证据，并通过 Evidence Check Agent 审核。

## MVP

MVP 只保留一个尖锐功能：

```text
给我一个 repo，
告诉我最可能被面试官问穿的地方，
每个问题附代码证据、参考答案、红旗回答和补坑建议。
```

## 用户界面

主界面：

- 左栏：会被问穿的问题。
- 右栏：Evidence Viewer。

左栏风险点包含：

- 风险等级：高 / 中 / 低。
- 问题标题。
- 面试官追问。
- 对应 claim。
- 参考答案。
- 红旗回答。
- 补坑建议。
- Evidence Check 状态。
- 回答框与持续追问聊天。

右栏 evidence viewer 包含：

- 文件路径。
- 行号。
- snippet。
- evidence reason。
- 多 reference 切换。

首页右上角：

- `Demo`
- `介绍`

介绍页用于 3 分钟演示讲解。

## Agent Pipeline

1. **Scout**
   获取仓库元数据、README、文件树和关键文件。

2. **Plan**
   规划研究维度和文件分配。

3. **Research**
   并行深读关键文件，提取代码事实和候选风险线索。

4. **Synthesize**
   合成项目理解地图。

5. **Generate Risk**
   生成 8 个及以上候选风险点，目标 20 个；证据不足可少于目标数。

6. **Evidence Bundle Builder**
   把 reference 解析成文件、行号和 snippet。

7. **Evidence Check Agent**
   检查 reference 是否充分且必要。

8. **Revision / Drop**
   `needs_revision` 尝试补证据或重写；`drop` 不进入最终结果。

9. **Final Sort**
   只保留通过审核或可接受的风险点，按高 / 中 / 低排序。

## Evidence Check Agent

输入：

- 候选 risks。
- evidenceBundle。
- repo understanding。
- paperCodeMap。
- 每个 risk 的 evidenceRefs 和 referenceAnswer。

输出：

- checkedRisks。
- 每个 risk 的 evidenceCheck。

字段：

```ts
type EvidenceCheck = {
  status: "pass" | "needs_revision" | "drop";
  sufficiency: "sufficient" | "partial" | "insufficient";
  necessity: "necessary" | "excessive" | "irrelevant";
  missingEvidence: string[];
  removedEvidenceRefs: EvidenceRef[];
  reason: string;
};
```

判定规则：

- 充分：reference 能支撑问题、claim、参考答案和红旗回答。
- 必要：每条 reference 都对判断风险点有贡献。
- 不充分：只有 README、只有文件名、只有泛模块，无法支撑具体追问。
- 不必要：重复、无关、只因关键词命中。

阻断规则：

- `pass` 进入最终结果。
- `needs_revision` 必须补 evidence 或重写。
- `drop` 不进入最终结果。

## 问题生成规则

问题必须是面试官口语。

必须问：

- 设计动机。
- 权衡取舍。
- 失败边界。
- 反事实替代方案。
- 内部实现约束。
- claim 和代码是否一致。
- 评测和数据是否支撑结论。

禁止问：

- “包含哪些部分”。
- “参数有哪些”。
- “请基于某文件说明”。
- 主问题围绕 HuggingFace / vLLM / transformers / 版本升级。
- evidence 没证明但题面直接声称的实现细节。

## API / Types

`AnalyzeResponse`：

- `risks: RepoInterviewRisk[]`
- `evidenceBundle: EvidenceDocument[]`

核心类型：

- `RepoInterviewRisk`
- `EvidenceRef`
- `EvidenceDocument`
- `EvidenceCheck`

新增：

- `/api/risk-chat`

输入：

- `riskId`
- `risk`
- `answer`
- `history`
- `evidenceRefs`
- `repoSummary`

输出：

- `reply`
- `followUpQuestion?`
- `referenceAnswer?`

## Test Plan

单元测试：

- evidence ref 行号 / snippet 解析。
- Evidence Check schema 解析。
- `pass / needs_revision / drop` 三类处理。
- 多余 evidence 删除。
- 审核后按高 / 中 / 低排序。
- 外部框架主问题 drop。
- 分布式路径与 varlen 互斥问题改写。

集成测试：

- `/api/analyze` 返回 `risks` 和 `evidenceBundle`。
- 每个最终 risk 至少有一个 evidence ref。
- `kaomian` 来源风险点必须通过 repo evidence check。
- `/api/risk-chat` 支持多轮追问。

浏览器验收：

- 页面无 Survey / Practice / Test 主入口。
- 主文案显示实际 `k`。
- 点击风险点后右侧 evidence 正确切换。
- 风险卡显示 Evidence Check 状态、参考答案、红旗回答、补坑建议。
- 同一风险点可持续追问多轮。
- Demo / 介绍切换正常。

## 演示要求

演示必须稳定：

- 使用 `deepseek-v4-flash` + non-thinking。
- 必要时为 demo repo 固定结果快照。
- 目标：1 分钟内进入可展示结果。

## 非目标

V1 不做：

- 独立 Practice。
- 整场 Test。
- 评分。
- 详尽复盘。
- 题集历史。
- 泛八股题库。
- JD 匹配。
- arXiv 解析。
- 登录。
- 大型数据库。
