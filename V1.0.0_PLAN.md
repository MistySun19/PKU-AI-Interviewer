# V1.0.0 计划：仓库理解优先的项目考核面试生成器

## 1. 当前判断

V1.0.0 的第一优先级不是 JD 匹配，也不是同时覆盖所有输入形态，而是先把 **GitHub 仓库理解** 做扎实。

原因：

- 真正的项目拷打首先来自材料本身，而不是岗位描述。
- 如果仓库理解不准，JD 只会把泛问题包装成岗位化问题。
- DevContext.AI 证明了“仓库分析 -> 报告 -> 面试问题”这条路线可行，但我们的目标要从软件工程仓库分析扩展到项目考核。
- 研究岗 / 算法岗 / 保研场景里，面试官会先确认候选人是否真的懂自己的项目，再切到原理、八股、实验和落地。

因此 V1.0.0 先做：

- 必要输入：GitHub 仓库链接。
- 核心能力：仓库抓取、文件筛选、结构化理解、可追问点审查、证据绑定问题生成。
- 题库层：`kaomian` 作为补充校准器，但不能盖过仓库理解。

暂时后置：

- JD / 岗位描述。
- arXiv 论文解析。
- 实时聊天式面试。
- `bagu-killer` 定时更新题库。

## 2. 一句话定位

> 输入 GitHub 仓库，系统先读懂项目目标、代码结构、核心模块、数据流和评测逻辑，再生成一份有证据来源的项目考核面试计划。

## 3. V1.0.0 核心架构

```text
必要输入：GitHub 仓库链接

        ↓

仓库抓取与文件筛选
        ↓
仓库结构化理解
        ↓
项目考核点审查
        ↓
检索 kaomian 高频八股
        ↓
连接仓库证据与八股
        ↓
生成项目考核面试计划
```

后续增强：

```text
V1.1：增加 JD / 岗位描述偏置
V1.2：增加 arXiv / 论文项目理解
V1.3：增加实时追问
V1.4：接入 bagu-killer 定时更新题库
```

## 4. 模块拆分

### 4.1 输入解析

V1.0.0 只要求处理 GitHub 仓库链接。

职责：

- 校验 GitHub URL。
- 解析 owner、repo、branch。
- 判断公开仓库是否可访问。
- 记录用户可选补充，例如“我负责的模块”或“目标方向”。

输出：

- `source_type = github`
- `repository_url`
- `owner`
- `repo`
- `branch`
- `user_focus`
- `domain_hint`

### 4.2 仓库抓取

职责：

- 获取 README。
- 获取文件树。
- 获取关键源码文件。
- 获取配置文件、测试文件、评测脚本、示例数据说明。
- 排除依赖目录、锁文件、构建产物、图片、二进制文件、大文件和自动生成文件。

优先级：

1. README / docs / examples。
2. 入口文件和核心模块。
3. 配置、依赖、脚本。
4. tests / eval / benchmark。
5. 和模型、agent、RAG、训练、推理、数据处理相关的文件。

### 4.3 仓库结构化理解

这一层是 V1.0.0 的核心，不允许跳过。

输出必须包含：

- 项目目标：这个仓库到底解决什么问题。
- 用户贡献假设：哪些部分最可能是候选人需要解释的工作。
- 技术栈：语言、框架、模型、服务、工具。
- 入口路径：怎么运行，主流程从哪里开始。
- 核心模块：每个模块负责什么。
- 数据流 / 状态流：输入、处理、输出怎么走。
- 模型或 agent loop：如果存在，拆出 loop、state、tool、memory、eval。
- 评测逻辑：是否有 test、eval、benchmark、metrics、demo case。
- 异常处理：失败、边界、日志、重试、fallback。
- 部署和生产化考虑：配置、环境变量、服务边界、推理成本。

### 4.4 项目考核点审查

审查智能体要像认真面试官、导师或 code reviewer 一样找可追问点。

GitHub 可出题点：

- 这个项目真正的技术难点在哪里。
- README 和代码是否一致。
- 核心模块为什么这么拆。
- 数据流为什么这么走。
- 关键实现是否有可解释设计。
- 有没有测试、评测和 bad case。
- 如果项目是 AI / Agent / RAG / RL，模型、数据、评测、推理链路是否讲得清。
- 如果上线使用，延迟、成本、稳定性、错误恢复怎么处理。
- 哪些地方最容易被问“为什么这样写”。
- 哪些地方会暴露“可能不是自己做的”。

### 4.5 kaomian 题库检索

V1.0.0 使用 `kaomian` 的 Markdown 题库快照，但它只能作为补充校准器。

推荐优先使用：

- `01_Top100_高频题.md`
- `03_Agent_RAG_Tool_Memory.md`
- `05_机器学习_大模型手撕.md`
- `06_项目拷打题.md`

检索输入：

- 仓库技术标签。
- 仓库审查出的风险点。
- 核心模块涉及的原理关键词。

检索输出：

- 相关高频题。
- 相关岗位 / 公司出现情况，如果题库里有。
- 题型。
- 可连接的项目证据。

### 4.6 仓库证据与八股连接

这是出题生成前的关键步骤。

系统不能直接把 `kaomian` 题拼到面试里，而要把题库问题改写成项目相关问题。

例子：

```text
仓库证据：代码里有 RAG 召回模块，但没有 eval 或 bad case 分析。
kaomian 召回：RAG 召回 bad case 怎么处理？
生成问题：你这个仓库里的 RAG 召回失败时，怎么判断是 chunk 策略、embedding 模型还是 rerank 逻辑的问题？
```

如果题库题找不到仓库证据，只能放到“补充八股”，不能进入主追问链。

## 5. 最终输出格式

V1.0.0 先输出“项目考核面试计划”，而不是完整聊天系统。

建议结构：

```text
1. 仓库摘要
2. 项目主流程理解
3. 核心模块地图
4. 面试官视角总结
5. 可出题点列表
6. 高频八股连接
7. 分层面试问题
8. 每题证据来源
9. 每题期望回答
10. 红旗回答
11. 追问链
12. 面试前补坑建议
```

每道题建议包含：

- 问题。
- 问题类型。
- 难度。
- 仓库证据来源。
- 为什么会问。
- 关联的高频题。
- 期望回答要点。
- 红旗回答。
- 后续追问。

## 6. V1.0.0 最小可行版本

为了尽快落地，V1.0.0 可以先做成“生成报告”：

1. 用户输入 GitHub 仓库链接。
2. 可选填写“我负责的模块 / 目标方向”，不接 JD。
3. 点击生成。
4. 系统输出一份 Markdown 项目考核面试计划。

暂时不做：

- JD / 岗位匹配。
- arXiv 论文解析。
- 实时聊天。
- 用户登录。
- 历史记录。
- 视频 / 语音。
- 自动运行 `bagu-killer`。
- 在线更新题库。

## 7. 后续版本

### V1.1：JD 偏置

- 增加 JD / 岗位描述输入。
- 把仓库理解结果映射到岗位能力要求。
- 输出“这个岗位最可能追问仓库里的哪些部分”。

### V1.2：arXiv / 论文项目理解

- 增加 arXiv 链接输入。
- 解析论文动机、方法、实验、指标、局限和相关工作。
- 把论文证据接入项目考核面试计划。

### V1.3：实时面试追问

- 用户可以选择某个问题开始连续追问。
- 每轮回答后给出评分和下一轮追问。
- 引入 answer evaluation 和 follow-up loop。

### V1.4：题库定时更新

- 定时运行 `bagu-killer`。
- 自动更新题库。
- 记录不同时间段高频题变化。

## 8. 风险和解决方案

### 风险 1：仓库太大

解决：

- 先读 README 和文件树。
- 只抓核心文件。
- 按文件类型、路径、import 关系和 README 提及频次打分。
- 大文件跳过。

### 风险 2：仓库理解太浅

解决：

- 输出必须包含项目主流程、核心模块、数据流和评测逻辑。
- 不允许只根据 README 生成问题。
- 问题必须引用仓库证据。

### 风险 3：题库和项目两张皮

解决：

- 每道主问题必须绑定仓库证据。
- 如果找不到证据，只能作为“补充八股”，不能进入主面试计划。

### 风险 4：问题太泛

解决：

- 每个问题必须包含“为什么问”和“证据来源”。
- 禁止输出没有材料依据的泛泛问题。

### 风险 5：AI / 研究项目被当成普通 Web 项目

解决：

- 仓库理解阶段必须识别模型、数据、agent loop、评测、推理和训练相关文件。
- 对 AI / Agent / RAG / RL 项目使用专门审查维度。
- 对保研 / 研究岗场景保留“为什么这么设计、实验是否可信、细节是否真懂”的追问方向。

## 9. 当前最重要的产品原则

- 仓库理解优先。
- 证据优先。
- 题库只是校准器，不是主线。
- JD 是后续偏置层，不是 V1.0.0 必要能力。
- 先生成高质量面试计划，再做实时聊天。
- 不要为了演示效果牺牲问题的可信度。
