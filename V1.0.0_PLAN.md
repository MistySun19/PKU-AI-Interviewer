# V1.0.0 计划：证据驱动的 AI 面试生成器

## 1. 当前判断

V1.0.0 不应该从“用户手填项目描述”开始，而应该从一手材料开始：

- arXiv 论文
- GitHub 仓库

JD 可以作为可选输入，用来调整面试问题的岗位相关性。

题库层先使用 `kaomian` 作为当前快照；`bagu-killer` 暂时不接入主流程，但作为未来定时更新题库的生产流水线。

## 2. 一句话定位

> 输入 arXiv 论文或 GitHub 仓库，系统先审查材料本身，再结合可选 JD 和高频面经题库，生成一份有证据来源的项目相关面试计划。

## 3. 核心架构

```text
必要输入：arXiv 链接 / GitHub 仓库链接
可选输入：JD / 岗位描述

        ↓

材料抓取与解析
        ↓
结构化理解
        ↓
审查可出题点
        ↓
检索 kaomian 高频八股
        ↓
连接项目证据与八股
        ↓
生成项目相关面试计划
```

长期题库更新：

```text
bagu-killer 定时运行
        ↓
生成新的题库快照
        ↓
刷新 kaomian / 检索索引
        ↓
AI Interviewer 消费新题库
```

## 4. 模块拆分

### 4.1 输入解析

职责：

- 判断输入类型：arXiv / GitHub。
- 解析可选 JD。
- 抽取目标岗位、方向、关键词。

输出：

- `source_type`
- `source_url`
- `jd_keywords`
- `target_role`
- `domain_tags`

### 4.2 材料抓取

arXiv：

- 获取标题、摘要、作者、分类、PDF / HTML 正文。
- 优先使用可解析正文，失败时退回 PDF 文本。
- 保留章节结构。

GitHub：

- 获取 README。
- 获取文件树。
- 筛选关键文件。
- 排除依赖目录、锁文件、构建产物、图片、大文件。

### 4.3 结构化理解

arXiv 输出：

- 研究问题
- 核心方法
- 关键假设
- 数据集
- 指标
- 实验设置
- 消融实验
- 局限
- 相关工作差异

GitHub 输出：

- 项目目标
- 技术栈
- 入口文件
- 核心模块
- 数据流 / 状态流
- 模型或 agent 流程
- 测试 / 评测
- 异常处理
- 部署和上线考虑

### 4.4 审查可出题点

审查智能体要找的是“真实面试官会抓住问的点”。

arXiv 可出题点：

- 方法为什么这么设计。
- 和 baseline 的差异是否真实。
- 数据和指标是否合理。
- 消融是否支撑结论。
- 局限是否会影响落地。
- 近期工作如何对比。

GitHub 可出题点：

- 关键模块为什么这么拆。
- 数据流为什么这么走。
- 异常和失败怎么处理。
- 评测怎么做。
- 上线后延迟、成本、稳定性怎么保证。
- 如果需求变化，架构怎么改。

### 4.5 kaomian 题库检索

V1.0.0 使用 `kaomian` 的 Markdown 题库快照。

推荐优先使用：

- `01_Top100_高频题.md`
- `03_Agent_RAG_Tool_Memory.md`
- `05_机器学习_大模型手撕.md`
- `06_项目拷打题.md`

检索输入：

- JD 关键词
- 材料标签
- 审查出的可出题点

检索输出：

- 相关高频题
- 出现公司
- 出现岗位
- 题型

### 4.6 项目证据与八股连接

这是最关键的一步。

系统不能直接把 `kaomian` 题拼到面试里，而要把题库问题改写成项目相关问题。

例子：

```text
材料证据：仓库里有 RAG 召回模块，但没有评测代码。
kaomian 召回：RAG 召回 bad case 怎么处理？
生成问题：你这个仓库里的 RAG 召回失败时，怎么判断是 chunk 策略、embedding 模型还是 rerank 逻辑的问题？
```

## 5. 最终输出格式

V1.0.0 先输出“项目相关面试计划”，而不是完整聊天系统。

建议结构：

```text
1. 材料摘要
2. 面试官视角总结
3. 可出题点列表
4. JD 相关性分析
5. 高频八股连接
6. 分层面试问题
7. 每题期望回答
8. 红旗回答
9. 追问链
10. 面试准备建议
```

每道题建议包含：

- 问题
- 问题类型
- 难度
- 证据来源
- 为什么会问
- 关联的高频题
- 期望回答要点
- 红旗回答
- 后续追问

## 6. V1.0.0 最小可行版本

为了尽快落地，V1.0.0 可以先做成“生成报告”：

1. 用户输入 GitHub 或 arXiv 链接。
2. 可选粘贴 JD。
3. 点击生成。
4. 系统输出一份 Markdown 面试计划。

暂时不做：

- 实时聊天。
- 用户登录。
- 历史记录。
- 视频 / 语音。
- 自动运行 `bagu-killer`。
- 在线更新题库。

## 7. 后续版本

### V1.1

- 增加实时面试聊天。
- 用户可以选择某个问题开始追问。
- 每轮回答后给出评分和追问。

### V1.2

- 接入向量检索。
- 把 `kaomian` 题库做成可检索索引。
- 支持更多方向的题库快照。

### V1.3

- 定时运行 `bagu-killer`。
- 自动更新题库。
- 记录不同时间段高频题变化。

## 8. 风险和解决方案

### 风险 1：arXiv 解析不稳定

解决：

- 优先解析 HTML / 文本。
- PDF 解析失败时使用摘要 + 用户粘贴补充。
- 保留章节级引用，不强求精确到行。

### 风险 2：GitHub 仓库太大

解决：

- 先读 README 和文件树。
- 只抓核心文件。
- 按文件类型和路径打分。
- 大文件跳过。

### 风险 3：题库和项目两张皮

解决：

- 每道题必须绑定项目 / 论文证据。
- 如果找不到证据，只能作为“补充八股”，不能进入主面试计划。

### 风险 4：问题太泛

解决：

- 每个问题必须包含“为什么问”和“证据来源”。
- 禁止输出没有材料依据的泛泛问题。

## 9. 当前最重要的产品原则

- 证据优先。
- 题库只是校准器，不是主线。
- JD 只是偏置，不是必要条件。
- 先生成高质量面试计划，再做实时聊天。
- 不要为了演示效果牺牲问题的可信度。

