# V1.0.0 计划：repo 细节拷打 MVP

## 1. 当前判断

V1.0.0 需要从完整平台收缩回一个最小、尖锐、可验证的闭环。

之前的 beta 已经包含 Survey、Practice、Test、追问、评分、复盘、历史记录、`kaomian` 题库连接、Repo Deep Research 和 paper-code 理解。能力很完整，但从 YC 视角看，风险也很明确：

> 还没有证明最小需求，就开始做完整平台。

当前真正要验证的问题是：

> 用户给一个 repo 后，系统能不能比 ChatGPT 加 GitHub 地址更准地抓到“面试官会问穿我的 8 个真实项目细节”？

因此 V1.0.0 不再以“完整面试训练平台”为目标，而以“8 个有代码证据的高风险追问点”为目标。

## 2. 一句话定位

> 输入 GitHub 仓库，系统先读真实代码证据，再找出面试官最可能追问候选人的 8 个项目细节；每个问题都附证据、提示、预期回答和红旗回答。

## 3. 与 ChatGPT + GitHub 地址的差异

本产品不是“AI 生成题目”。

它要解决的是：

1. **细节是否真实**：问题必须来自仓库里真实存在的代码、配置、脚本、数据处理或评测逻辑，不能凌空幻想。
2. **细节是否够细**：问题不能停在“这个项目用了什么模型 / 技术栈”这类泛层面，而要问到具体方法实现、参数选择、训练入口、数据流、评测路线、复现缺口或 claim-code 对齐风险。
3. **是否能拷打 ownership**：问题要能暴露候选人是不是真的做过、读过、调过、复现过这个项目。

如果输出只是“10 道项目相关问题”，那它和 ChatGPT 没有足够差异。

如果输出是“8 个带代码证据、问得很细、能暴露没做过的追问点”，才是 V1 的价值。

## 4. V1.0.0 MVP 功能范围

保留两个模块：

- **Survey**：repo 证据地图。展示系统读到了哪些关键文件、方法、训练 / 推理 / 评测入口、数据流、风险点和候选追问点。
- **Test**：8 个追问点的轻量测试。用户逐题回答，也可以打开提示；每题展示代码证据、预期回答要点和红旗回答。

V1 暂不做：

- Practice 独立练习模块。
- 实时连续追问。
- 自动评分。
- 详尽复盘。
- 题集 / 聊天 / 复盘历史。
- `kaomian` / 八股题库连接。
- `bagu-killer` 题库更新流水线。
- arXiv 论文解析。
- JD / 岗位偏置。
- 登录、数据库、语音、视频。

## 5. MVP 核心架构

```text
必要输入：GitHub 仓库链接
可选输入：用户补充的负责模块 / 目标方向

        ↓

仓库抓取与文件筛选
        ↓
repo deep research / paper-code 理解
        ↓
证据地图合成（Survey）
        ↓
高风险追问点审查
        ↓
选择 8 个最可能被问穿的细节
        ↓
生成 Test：问题 + 证据 + 提示 + 预期回答 + 红旗回答
```

## 6. 输入约束

必要输入：

- GitHub 仓库链接。

可选输入：

- 用户负责的模块。
- 用户目标方向。
- 用户希望重点检查的路径。

约束：

- 不把手填项目描述作为主入口。
- 不接 JD。
- 不接 arXiv。
- 不接八股题库。

用户补充信息只能用于聚焦 repo 证据，不能替代 repo 证据。

## 7. Survey 输出

Survey 是为了让用户相信系统真的读了 repo，而不是为了做长报告。

Survey 必须包含：

- 仓库摘要。
- `paper-code` / `general-code` / `unknown` 判断。
- Paper claim / README claim 摘要。
- 训练、推理、评测复现路线。
- 核心方法代码地图。
- 数据处理路径。
- 配置与关键超参。
- 实验与评测证据地图。
- 复现风险。
- 候选追问点池。

Survey 不追求完整论文解读，不追求漂亮长文。它的任务是支撑后面的 8 个追问点。

## 8. Test 输出

Test 固定生成 8 个问题。

每题必须包含：

- `question`：面试官会怎么问。
- `evidence`：文件路径、函数 / 类 / 配置 / 脚本、相关证据说明。
- `why_it_matters`：为什么这个点能检验候选人是否真正懂项目。
- `expected_points`：预期回答要点。
- `hint`：用户可以主动打开的提示。
- `red_flag_answer`：危险回答或泛泛回答示例。
- `follow_up_seed`：如果后续恢复追问，这个点可以继续往哪里问。

Test 允许用户看提示，因为当前 MVP 的重点不是制造考试压力，而是验证系统是否能抓到真实细节。

## 9. 8 个追问点选择规则

优先级从高到低：

1. README / paper claim 和代码实现之间的关键连接。
2. 训练入口、loss、optimizer、scheduler、seed、checkpoint、config。
3. 推理入口、模型加载、采样 / 后处理、失败边界。
4. 数据来源、split、preprocess、dataloader、tokenizer、label、泄漏风险。
5. eval、metric、baseline、ablation、benchmark、bad case。
6. agent / RAG / tool / memory / policy / reward / loop 等核心链路。
7. 复现命令、环境依赖、权重或数据下载缺口。
8. 最容易暴露“这不是你做的”的具体实现细节。

禁止：

- 没有证据的问题。
- 只根据仓库名或 README 猜的问题。
- 泛泛八股题。
- 代码质量 / 可维护性题作为主线。
- 为了凑 8 题而降低证据标准。

如果高质量追问点不足 8 个，系统必须说明不足原因，而不是编满。

## 10. 成功标准

V1.0.0 成功，不要求平台完整。

成功标准是：

1. 用户输入 GitHub 仓库链接。
2. 系统能抓取并结构化理解 GitHub 仓库。
3. 对论文 / AI 项目制仓库，系统能识别 paper-code 模式。
4. Survey 能展示关键证据地图，而不是泛摘要。
5. Test 能输出最多 8 个高风险追问点。
6. 每个追问点都有具体代码证据。
7. 每个追问点都足够细，能检验项目 ownership。
8. 每个追问点都有提示、预期回答和红旗回答。
9. 目标用户认为这些点比直接问 ChatGPT 更像真实面试官。

## 11. 验证计划

先用人工评审验证，不先扩功能。

评审对象：

- 5 到 10 个 AI / 算法 / paper-code repo。
- 其中至少包含训练型、RAG / Agent 型、benchmark / eval 型、复现型 repo。

每个 repo 只评估 8 个追问点：

- 证据真实性：是否真的来自代码。
- 细节颗粒度：是否具体到方法、参数、数据、评测或复现。
- 面试命中感：熟悉项目的人是否觉得“这个真可能被问”。
- 红旗有效性：红旗回答是否能暴露没真正理解。

暂不评估：

- UI 完整度。
- 历史记录。
- 评分算法。
- 复盘质量。
- 题库召回。

## 12. 后续版本方向

只有当 MVP 被证明有效后，再恢复平台能力。

可能顺序：

1. Test 连续追问：基于 8 个点做 1 到 2 轮追问。
2. 回答评价：只评价是否覆盖证据链和关键概念。
3. 复盘：围绕 8 个点给补坑计划。
4. Practice：把提示和参考答案做成独立练习模式。
5. 历史记录：保存 repo 分析和用户答题记录。
6. `kaomian` / 八股：作为补充层，只在 repo 证据足够强时连接。
7. arXiv：补充论文证据。
8. JD：做岗位偏置。

## 13. 当前不要做

- 不要先做语音 / 视频。
- 不要先做登录。
- 不要先做数据库。
- 不要先做 JD 匹配。
- 不要先做 arXiv 论文解析。
- 不要先做泛题库页面。
- 不要先做 `kaomian` / 八股连接。
- 不要做 `bagu-killer` 集成。
- 不要把 Practice、评分、复盘、历史作为 V1 成功标准。
- 不要在没有证据来源的情况下生成“项目相关问题”。

## 14. 当前最重要的产品原则

- 问题少一点，但必须准。
- 证据链硬一点，文案可以朴素一点。
- 先证明用户需要“被 repo 真实细节拷打”。
- 先解决和 ChatGPT 的差异，再做平台完整度。
- Test 可以看提示，因为 MVP 的目标是验证问题质量，不是模拟高压考试。
